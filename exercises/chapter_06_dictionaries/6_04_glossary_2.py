glossary = {
    'python': 'A high-level programming language known for its readability and simplicity.',
    'if statements': 'Used to perform conditional tests and execute code ' 
    'based on whether a condition is true or false.',
    'variables': 'Containers for storing data values that can change.',
    'lists': 'Ordered collections of items that can be modified (mutable).',
    'tuples': 'Ordered collections of items that cannot be modified (immutable).'
    }


for term,description in glossary.items():
    print(f"\nFor '{term}' the description is:\n{description}")

# definition = glossary['python']
# print(f"\nPython's definition:\n{definition}\n")

# definition = glossary['if statements']
# print(f"If statements's definition:\n{definition}\n")

# definition = glossary['variables']
# print(f"Variable's definition:\n{definition}\n")

# definition = glossary['lists']
# print(f"Lists' definition:\n{definition}\n")

# definition = glossary['tuples']
# print(f"Tuple's definition:\n{definition}\n")